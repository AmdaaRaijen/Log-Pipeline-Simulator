services:
  pipeline-simulator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pipeline-simulator
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
